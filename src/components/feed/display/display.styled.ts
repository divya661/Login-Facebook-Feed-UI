import styled from "@emotion/styled";

const StyledDisplay = styled.section`
  display: flex;
  flex-grow: 1;
  margin-top: 16px;
  overflow-y: auto;
  overflow-x: hidden;
`;

export { StyledDisplay };
